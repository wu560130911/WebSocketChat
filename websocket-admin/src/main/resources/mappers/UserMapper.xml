<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wms.studio.mapper.UserMapper">

	<cache></cache>

	<select resultType="User" parameterType="string" id="findUserById"> select *
		from `user` where id = #{id} </select>
	<select resultType="User" parameterType="string" id="findUserByEmail"> select *
		from `user` where email = #{email} </select>
	<insert id="addUser" parameterType="User" keyProperty="id">
		insert
		into user
		(id,name,password,email,register_time,disable,salt,role,disable_reason)
		values
		(#{id},#{name},#{password},#{email},#{register_time},#{disable},#{salt},#{role},#{disable_reason})
	</insert>
</mapper>